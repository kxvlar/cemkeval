{
  "name": "callrunner",
  "version": "1.0.0",
  "description": "AI agents that call places and do things for you.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --watch --no-warnings server.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
